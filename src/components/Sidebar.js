
// Sidebar component is deprecated in favor of top navbar. Keeping file as no-op to avoid import errors if any stale references exist.
import React from 'react';

const Sidebar = () => null;

export default Sidebar;
